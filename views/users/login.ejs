<!DOCTYPE html>
<html lang="es">
  <%- include('../partials/head.ejs') %>

    <%- include('../partials/header.ejs') %>
    <main class="form-section">
    <div class="container">
      <h2>Iniciar sesión</h2>
      <form action="/users/login" method="post">

        <div class="form-group">
          <label for="email">Email:</label>
          <input type="text" name="email" placeholder="tu@correo.com" id="email" autofocus
          value="<%= locals.oldData ? oldData.email : "" %>" /> <% if
          (locals.errors && errors.email) { %>
          <p class="text-danger"><%- errors.email.msg %></p>
          <% } %>
          <span id="email-error"></span>
        </div>

        <div class="form-group">
          <label for="password">Contraseña:</label>
          <input type="password" name="password" id="password" />
          <% if (locals.errors && errors.password) { %>
          <p class="text-danger"><%- errors.password.msg %></p>
          <% } %>
          <span id="password-error"></span>
        </div>

        <div class="checkbox">
          <input type="checkbox" id="rememberMe" name="rememberMe" />
          <label for="rememberMe">Recordarme</label>
        </div>

        <input type="submit" value="Iniciar sesión"></input>
        
        <div class="form-group">
          <p>¿Aún no tienes cuenta? <a href="/users/register">Registrate</a></p>
        </div>
        <a href="/users/recoverpassword" id="forgotPassword"
          >¿Olvidaste tu contraseña?</a
        >
      </form>
    </div>
  </main>
    <%- include('../partials/footer.ejs') %>
    <script src="./js/login.js"></script>
  </body>
</html>
